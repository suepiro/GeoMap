<%= form_for(@post_picture) do |f| %>
  <% if @post_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_picture.errors.count, "error") %> prohibited this post_picture from being saved:</h2>

      <ul>
      <% @post_picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
